# Módulo: mod_Teleporte (Navegação Instantânea)

**Data**: 29/01/2026
**Status**: Operacional
**Autor**: Mestre em VBA
**Propósito**: Acabar com a era das trevas de rolar abas infinitas no Excel.

## 🛠️ O Código (Bisturi de Navegação)

```vba
Attribute VB_Name = "mod_Teleporte"
Option Explicit

' ==============================================================================
' MÓDULO: mod_Teleporte
' DESCRIÇÃO: Sistema de busca e ativação de planilhas por índice ou nome.
' ATALHO SUGERIDO: Ctrl + Shift + F
' ==============================================================================

Public Sub Teleportar()
    Dim termo As String
    Dim ws As Worksheet
    Dim listaEncontradas As String
    Dim contador As Integer
    Dim matchExato As Worksheet
    Dim escolha As String
    
    ' 1. Interface de Entrada
    termo = InputBox("Destino? (Número ou Parte do Nome):", "Teleporte Quântico")
    If Trim(termo) = "" Then Exit Sub
    
    ' 2. Verificação de Índice Numérico
    If IsNumeric(termo) Then
        If Val(termo) > 0 And Val(termo) <= ActiveWorkbook.Sheets.Count Then
            ActiveWorkbook.Sheets(Val(termo)).Activate
            Exit Sub
        End If
    End If
    
    ' 3. Busca Fuzzy (Nome ou CodeName)
    termo = UCase(Trim(termo))
    contador = 0
    listaEncontradas = "Múltiplos destinos encontrados:" & vbNewLine & vbNewLine
    
    For Each ws In ActiveWorkbook.Sheets
        ' Busca no Nome da Aba (Name) e no Nome do Objeto (CodeName)
        If InStr(1, UCase(ws.Name), termo) > 0 Or InStr(1, UCase(ws.CodeName), termo) > 0 Then
            contador = contador + 1
            If contador = 1 Then Set matchExato = ws
            
            listaEncontradas = listaEncontradas & "[" & ws.Index & "] " & ws.Name & vbNewLine
            
            ' Se for match idêntico, não perde tempo e vai direto
            If UCase(ws.Name) = termo Then
                ws.Activate
                Exit Sub
            End If
        End If
    Next ws
    
    ' 4. Resolução de Conflitos
    If contador = 0 Then
        MsgBox "Setor '" & termo & "' não encontrado. Recalibrando sensores...", vbExclamation
    ElseIf contador = 1 Then
        matchExato.Activate
    Else
        ' Desambiguação
        escolha = InputBox(listaEncontradas & vbNewLine & "Digite o NÚMERO do destino:", "Ambiguidade Detectada")
        
        If IsNumeric(escolha) Then
            If Val(escolha) > 0 And Val(escolha) <= ActiveWorkbook.Sheets.Count Then
                ActiveWorkbook.Sheets(Val(escolha)).Activate
            End If
        End If
    End If
End Sub
```

## 🚀 Instruções de Instalação

1.  **Inserção**: No VBA (`Alt+F11`), vá em `Inserir > Módulo` e cole o código acima.
2.  **Batismo de Atalho**:
    *   Volte ao Excel.
    *   `Alt + F8` > Selecione `Teleportar`.
    *   Clique em `Opções`.
    *   Digite `F` (maiúsculo) para configurar **Ctrl + Shift + F**.
3.  **Uso**: Aperte o atalho, digite "PLT" e veja a mágica acontecer. Se quiser ir para a primeira aba, digite "1". Simples assim.

---
*Nota do Mestre: Se você digitar algo e não for para lugar nenhum, a culpa provavelmente é do estagiário que nomeou as abas com espaços invisíveis no final. Mas meu código limpa o `Trim()`, então até contra a incompetência alheia estamos protegidos.*
