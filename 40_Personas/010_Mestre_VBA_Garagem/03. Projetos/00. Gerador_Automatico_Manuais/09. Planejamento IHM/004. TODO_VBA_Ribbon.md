# Lista de Pendências - Expansão IHM (VBA/Ribbon)

Este documento rastreia o que falta para que as novas abas (Despaletizador e Desencaixotadora) funcionem plenamente.

## 1. Mapeamento de Planilhas (VBA)
As macros `Mec`, `El` e `IHM` (índices 10 e 11) estão com placeholders.
- [x] Criar as planilhas de **Estrutura, Elétrica e IHM** para o Despaletizador (DPL).
- [x] Criar as planilhas de **Estrutura, Elétrica e IHM** para a Desencaixotadora (DPK).
- [x] Substituir `PLANILHA_PENDENTE_...` no módulo `VBA_Ribbon` pelos `CodeNames` reais (ex: `Planilha55`).

## 2. Implementação na NewEngine_V5
Os botões de "Gerar Manual" (Ger10 e Ger11) chamam métodos que ainda não existem.
- [ ] Criar `Sub Gerar_DPL()` no módulo `mod_NewEngine_V5`.
- [ ] Criar `Sub Gerar_DPK()` no módulo `mod_NewEngine_V5`.
- [ ] Garantir que a lógica de seleção de capítulos suporte as novas siglas.

## 3. Interface e Diálogos (UserForms)
- [ ] **UserForm Exceções**: Validar se a lista de exceções carregada para "DPL" e "DPK" é a mesma de "PLT" e "PCK".
- [ ] **UserForm Info**: Garantir que as informações de versão e máquina aceitem os novos tipos de equipamento.

## 4. XML do Ribbon (CustomUI)
- [ ] Validar se os IDs no XML (`TabDPL`, `TabDPK`, etc.) batem exatamente com o que foi definido nos Callbacks do VBA.
