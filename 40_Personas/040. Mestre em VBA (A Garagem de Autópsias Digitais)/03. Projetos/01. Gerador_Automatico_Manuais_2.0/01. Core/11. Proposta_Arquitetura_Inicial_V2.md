---
tags:
  - PLAN
  - ARQUITETURA
---
# 🏗️ Proposta de Arquitetura Inicial V2

> **Objetivo**: Migrar o conhecimento da Pasta 14 (V1) para a estrutura organizada do V2, minimizando complexidade.

## 1. Mapeamento V1 -> V2

### 📂 Para `01. Core` (Documentação & Specs)
Aqui ficam as "Regras do Jogo". Consolidaremos os vários arquivos picados em documentos mestres.

*   **`SPEC_UI_Cockpit.md`**:
    *   *Origem*: `1.1. Estrutura_UI_Avancada` + `1.3` + `1.4`.
    *   *Conteúdo*: Unificar toda a especificação visual (Zonas A, B, C) em um único arquivo de referência.
*   **`SPEC_DataModel.md`**:
    *   *Origem*: `2.1. Tabelas_de_Dados`.
    *   *Conteúdo*: Esquema das tabelas Excel (`DB_Models`, `DB_Mapping`) e como elas se relacionam.
*   **`PLAN_Refatoracao.md`**:
    *   *Origem*: `60.1` + `50.1`.
    *   *Conteúdo*: Roteiro técnico de implementação (o que codar primeiro).

### 📂 Para `02. Tools` (Implementação)
Aqui fica o código real. Abandonaremos a numeração `70.x` por nomes semânticos.

*   **`src/vba/mod_Shared.bas`** (Antigo `70.1`)
*   **`src/vba/mod_UI_Controller.bas`** (Fusão de `70.2` e `70.3`? Ou manter separado?)
    *   *Sugestão*: Manter separado por Zonas se o código for grande, mas com nomes claros: `mod_UI_ZoneA.bas`, `mod_UI_ZoneB.bas`.
*   **`data/schema_example.json`** (ou `.xlsx`)
    *   Representação física dos dados de `2.1`.

### 🗑️ Para o Arquivo Morto (Ignorar/Resumir)
Arquivos de "Debug" e "Logs de Erro" pontuais não devem ser migrados como arquivos. O conhecimento deles deve ser incorporado no código novo.

*   `40.1`, `40.2`, `3.1`: Devem virar comentários no código ou itens de atenção no `PLAN_Refatoracao`.

## 2. Estrutura de Pastas Sugerida (V2)

```text
01. Core/
├── 12. SPEC_UI_Cockpit.md        (A "Bíblia" da Interface)
├── 13. SPEC_DataModel.md         (A Estrutura dos Dados)
└── 14. PLAN_Migration_Strategy.md (O Plano de Ataque)

02. Tools/
├── src/
│   └── vba/
│       ├── mod_Shared.bas
│       ├── mod_UI_Core.bas
│       └── ...
└── data/
    └── db_mockup.xlsx
```

## 3. Próximos Passos
1.  Criar `12. SPEC_UI_Cockpit.md` unificando os arquivos `1.x` do V1.
2.  Criar `13. SPEC_DataModel.md` trazendo a tabela do `2.1`.
3.  Iniciar a codificação limpa em `02. Tools`, consultando os módulos `70.x` apenas como referência de lógica, não para copy-paste cego.
