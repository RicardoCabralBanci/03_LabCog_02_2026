# 0001. Afazeres - [[22-12-2025]]

**Foco**: Conexão Excel -> Python -> SQLite

1.  [x] **Investigação de Dados (Excel)**:
    *   Descobrir como exportar de forma legível (CSV/JSON). (Concluído: `mod_Export_Final` via Base de Dados).
    *   Mapear lógica de seleção "Yes/No". (Concluído: Coluna B da aba Base de Dados).

2.  [ ] **Sincronização (Python)**:
    *   [ ] Refatorar `orchestrator.py` para ler o novo formato `CATEGORY;KEY;VALUE`.
    *   [ ] Corrigir problema de encoding (Latin-1 vs UTF-8).
    *   [ ] Remover duplicação de imports no código Python.

3.  [ ] **Melhoria Técnica (C#)**:
    *   [ ] Alterar entrada do `WordStitcher` para ler um arquivo de lista (`file_list.txt`) em vez de argumentos CLI, para evitar estouro de limite de caracteres em projetos grandes.

4.  [ ] **Deployment**:
    *   [ ] Recompilar `orchestrator.exe` e `NewGerador.exe` (se necessário).
    *   [ ] Testar fluxo completo: Excel -> CSV -> Python -> C# -> DOCX.
