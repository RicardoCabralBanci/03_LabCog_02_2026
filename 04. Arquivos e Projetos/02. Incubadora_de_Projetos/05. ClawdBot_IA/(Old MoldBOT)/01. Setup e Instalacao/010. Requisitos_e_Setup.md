# 🛠️ Setup do ClawdBot: O Ritual de Invocação

**Status**: 📝 Documentação Prévia
**Repositório Oficial**: [github.com/clawdbot/clawdbot](https://github.com/clawdbot/clawdbot)

---

## 📋 Pré-Requisitos do Sistema (Windows Lab)

Para rodar este agente de forma estável no nosso ambiente Windows, precisamos preparar o terreno:

### 1. O Subsolo (WSL2)
O ClawdBot prefere o ambiente Linux. No Windows, a melhor forma é usar o **WSL2 (Windows Subsystem for Linux)** com Ubuntu.
*   *Por que?* Scripts de automação e manipulação de arquivos fluem melhor no bash do que no PowerShell.

### 2. A Ferramenta (Node.js)
*   **Versão Exigida**: Node.js **v22** ou superior.
*   **Gerenciador de Pacotes**: `pnpm` (Recomendado pelos criadores pela velocidade) ou `npm`.

### 3. As Chaves do Portal (APIs)
O ClawdBot é o "corpo", mas precisa de um "espírito" (LLM) para pensar.
*   **Principal (Recomendada)**: Anthropic API Key (Claude 3.5 Sonnet é o ideal).
*   **Opcional**: OpenAI API Key (GPT-4o).

---

## 🚀 Roteiro de Instalação (Draft)

1.  **Instalar WSL2** (Se não tiver): `wsl --install` no PowerShell Admin.
2.  **Instalar Node v22 no WSL**:
    ```bash
    curl -fsSL https://fnm.vercel.app/install | bash
    fnm install 22
    ```
3.  **Clonar o Repositório**:
    ```bash
    git clone https://github.com/clawdbot/clawdbot.git
    cd clawdbot
    ```
4.  **Configurar Variáveis (.env)**:
    *   Colocar as chaves de API.
    *   Definir quais apps de chat ele vai ouvir (Telegram, Discord).
5.  **Instalar Dependências**: `pnpm install`
6.  **Rodar**: `pnpm start`

---

## ⚠️ Notas do Bibliotecário
*   **Atenção**: Este bot tem permissão para **ler e editar arquivos**. Devemos rodá-lo em uma pasta isolada ou configurar permissões estritas para ele não apagar nossa biblioteca por engano!
*   **Custo**: A API da Anthropic é paga por uso (tokens). Monitore os custos no início.
